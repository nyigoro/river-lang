# Node / TypeScript
node_modules/
compiler/ts-frontend/dist/

# Rust
**/target/

# C / C++ builds
build/
cmake-build-*/
*.o
*.obj
*.dll
*.so
*.dylib

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Generated artifacts
*.rvr

# Core IP - Geologist Backend
# These files contain the spatial mapping algorithm, path audit logic,
# and binary emitter implementation. Do not publish until patent filed.
# The scaffold stubs (empty function signatures) ARE committed.
# Only the real implementations are excluded.
backend/rust/crates/geologist-backend/src/spatial_mapper_impl.rs
backend/rust/crates/geologist-backend/src/path_auditor_impl.rs
backend/rust/crates/geologist-backend/src/emitter_impl.rs

# Full compiled geologist binary
backend/rust/target/

# Core IP - C Runtime Internals
# The header (river_hw.h) is public. The implementations below are private
# until the silicon architecture is filed.
runtime/c/src/river_spawn_impl.c
runtime/c/src/river_enclave_wipe_impl.c

# Master Spec
RIVER-LANG_v1.1_MASTER_SPEC.md
docs/SPEC.md

# HDL Silicon Layer
hdl/cpp/src/drc_engine.cpp
hdl/cpp/src/hdl_codegen.cpp

# Local secrets
.env
*.salt
*.session
*.rvr.key

# Compiled binaries
runtime/c/build/
runtime/c/build-*/

# LSP build output
tooling/lsp/dist/
