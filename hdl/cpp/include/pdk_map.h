#ifndef RIVER_PDK_MAP_H
#define RIVER_PDK_MAP_H

// Replace values below with partner PDK cell names before tape-out.
// Do not change the macro keys.

#define RIVER_CELL_NODE "river_node"
#define RIVER_CELL_WIRE "wire"
#define RIVER_CELL_UPSTREAM_PORT "cry_out"
#define RIVER_CELL_DOWNSTREAM_PORT "blood_out"
#define RIVER_CELL_RESERVOIR "river_reservoir"

#endif // RIVER_PDK_MAP_H
